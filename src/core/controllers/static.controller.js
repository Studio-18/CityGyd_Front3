(function (module) {
  'use strict'
  //TODO:

  staticController.$inject = ['$state', '$scope', '$http' ];

  function staticController($state, $scope, $http) {
    var vm = this

  }
    module.controller('static.controller', staticController);
})(angular.module('Citygyd.Controllers'));
